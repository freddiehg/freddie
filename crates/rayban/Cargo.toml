[package]
name = "rayban"
description = "Mutable resolved paths (composed lenses and prisms) into a single-owner tree (work in progress; name reservation)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
