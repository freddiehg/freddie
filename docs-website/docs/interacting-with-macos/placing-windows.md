---
title: Placing Windows
sidebar_position: 5
---

# Placing Windows

In the resize layer, `up` maximizes a window, `right` resizes to the right half, and `left` resizes to the left half.

TODO: how `freddie_windows` finds the focused window through the accessibility API, and what it sets.

TODO: screen coordinates, multiple displays, and where the menu bar goes in the math.

TODO: apps that refuse to be resized.
