# freddie CLI plan

Placeholder for the `freddie` binary's command surface. Only one fact is pinned so far.

## Commands

- `freddie ready` — must exist. (Purpose to be specified; it is the command that reports/establishes that freddie is up and ready. Probably an alias for `freddie status`)
- `freddie dynamite` - clear state?

Everything else (subcommands, flags, output format) is TBD.
